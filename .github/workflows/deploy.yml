name: Trigger deploy

on:
  push:
    branches: [ main ]

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Log push event
        run: echo "Push detected op main branch. VPS pulled automatisch."
